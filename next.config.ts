import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  experimental: {
    // Enable if you need React compiler optimizations
    // reactCompiler: true,
  },
  images: {
    // If you have any external images
    // remotePatterns: [],
  },
}

export default nextConfig
