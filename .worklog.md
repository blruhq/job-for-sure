# Worklog

## 2026-07-24 — Neumorphism Apple Vision Pro Recalibration (specs/neumorphism-apple-vp-recalibration.md)
**Task:** Extract all hardcoded neumorphic shadow rgba values into CSS custom properties in `:root`, then update every `.neuro-*` class to reference those variables. Recalibrated opacity values to Apple Vision Pro profile (dark 0.15-0.25, light 0.15-0.20).

**Changes (1 file):**
- `src/app/globals.css` — Added 11 CSS custom properties (`--neuro-dark-card`, `--neuro-dark-card-hover`, `--neuro-dark-inset`, `--neuro-dark-pill`, `--neuro-dark-pill-hover`, `--neuro-dark-pill-active`, `--neuro-dark-modal`, `--neuro-dark-chat`, `--neuro-dark-chat-hover`, `--neuro-light-base`, `--neuro-light-hover`) in `:root`. Updated 9 `.neuro-*` class groups to reference variables. Dark mode `.dark .neuro-*` section untouched.

**Verification:** `npx tsc --noEmit` ✓ (0 errors), `pnpm build` ✓ (exit 0)

## 2026-07-24 — [Quick Mode] Normalize hardcoded px font sizes to rem-based tokens
**Task:** Replace all `text-[8px]`→`text-[10px]`, `text-[9px]`→`text-[10px]`, `text-[11px]`→`text-xs`, `text-[12px]`→`text-xs`, `text-[13px]`→`text-sm` across 21 components.

**Changes (21 files):**
- `src/app/components/ats/ats-view.tsx`
- `src/app/components/chat/upload-card-message.tsx`
- `src/app/components/chat/chat-view.tsx`
- `src/app/components/chat/job-preview.tsx`
- `src/app/components/interview/interview-summary.tsx`
- `src/app/components/interview/interview-view.tsx`
- `src/app/components/interview/interview-session.tsx`
- `src/app/components/interview/interview-setup.tsx`
- `src/app/components/marketing/interview-section.tsx`
- `src/app/components/resume/resume-detail.tsx`
- `src/app/components/resume/job-search-panel.tsx`
- `src/app/components/resume/resume-copilot.tsx`
- `src/app/components/resume/cover-letter-editor.tsx`
- `src/app/components/resume/resume-preview.tsx`
- `src/app/components/pipeline/applications-view.tsx`
- `src/app/components/pipeline/job-detail-panel.tsx`
- `src/app/components/pipeline/job-notes.tsx`
- `src/app/components/pipeline/area-intelligence.tsx`
- `src/app/components/pipeline/company-intelligence.tsx`
- `src/app/components/pipeline/timeline.tsx`
- `src/app/components/layout/` (0 stragglers found)

**Verification:** `tsc --noEmit` (0 errors), `pnpm build` (success)
**Outcome:** Success — 232 insertions, 219 deletions across 21 files

## 2026-07-24 — Fix Kanban Card Left-Edge Clipping
**Task:** Fix text clipping on left edge of job cards in `/en/applications` Kanban board.

**Changes (1 file):**
- `src/app/components/pipeline/applications-view.tsx`
  - **Edit 1** (line 75): Added `min-w-0 overflow-hidden` to `DraggableJobCard` className — allows card to shrink within flex context.
  - **Edit 2** (line 525): Overrode Button base styles (`inline-flex justify-center gap-2 whitespace-nowrap`) with explicit flex overrides — `flex flex-col items-start justify-start gap-0 whitespace-normal min-w-0` — prevents center-clipping of text.
  - **Edit 3** (lines 511-512): Fixed cards container — added `flex-1 min-h-0 min-w-0 overflow-x-hidden px-3 pb-2 pt-3` — prevents horizontal clip boundary, gives shadow room, enables proper vertical scroll.

**Verification:** `npx tsc --noEmit` ✓ (0 errors), `pnpm lint` ✓ (0 errors, 1 pre-existing warning in unrelated file)



## 2026-07-23 — [Quick Mode] P0 Audit Fixes: All 4 Criticals Resolved

**Task:** Fix 4 P0 critical issues from codebase audit (`docs/AUDIT-2026-07-23.md`).

**P0 #1 — Dependency Vulnerabilities (commit `ee0f5cd`)**
- `next`: 16.2.10 → 16.2.11 (4 HIGH CVEs: middleware bypass, DoS, SSRF)
- `drizzle-orm`: 0.42.0 → 0.45.2 (1 HIGH CVE: SQL injection)
- `sharp`: 0.34.5 → 0.35.3 (4 HIGH libvips CVEs)
- Result: 14 vulns → 3 vulns (0 HIGH remaining)

**P0 #2 — CI/CD Pipeline (commit `a2f7d98`)**
- Created `.github/workflows/ci.yml` — lint + typecheck + unit tests + build (parallel jobs, pnpm cache, Node 22)
- Created `.github/workflows/security.yml` — `pnpm audit --audit-level=high` on push + weekly schedule

**P0 #3 — XSS Sanitization (commit `edab6e8`)**
- Investigation result: `streamdown` already sanitizes via `rehype-sanitize` (default schema)
- No new dependency needed. Documented the built-in protection in `markdown.tsx`.

**P0 #4 — AGENTS.md & Docs Fixes (committed by engineer)**
- Fixed all paths: `app/` → `src/app/` across AGENTS.md, docs/architecture.md, 2 ADRs, docs/glossary.md
- Removed dead `store.tsx` references, updated to Zustand v5 + TanStack Query v5
- Fixed path aliases: `~/` → `./src/app/`, `@/` → `./src/`

## 2026-07-23 — [Quick Mode] Spacing & Readability: Resume Wizard + Upload Modal

**Task:** Fix cramped fonts and cards in the template gallery, build wizard, and upload modal.

**Changes (3 files):**
- `template-gallery.tsx` — Grid 5→2/3 col, thumbnails 80px→96px, name `text-[10px]`→`text-xs`, description `text-[8px]`→`text-[11px]`, card padding `p-2`→`p-2.5`, check badge `h-4 w-4`→`h-5 w-5`, spacing tweaks.
- `build-wizard.tsx` — Dialog `max-w-xl`→`max-w-2xl`, header/body/footer padding increased, STEP/title/labels 10px→11px, title `text-sm`→`text-base`, template step `space-y-3`→`space-y-4`, footer primary buttons `px-4 py-1.5 text-xs`→`px-5 py-2 text-sm`.
- `upload-modal.tsx` — Inner content `space-y-4`→`space-y-5`, upload zone `p-8`→`p-10`, file type/OR/AI description text sizes bumped to `text-xs`.

**Verification:**
- `npx tsc --noEmit` — 0 errors
- `pnpm lint` — 0 errors, 2 pre-existing warnings
- Commit: `bb4ceae` — `fix: improve spacing and readability in resume upload modal and template wizard`

## 2026-07-23 — UX Polish: Soft UI + shadcn Finish + Onboarding Card Fix

**Task:** Finish shadcn migration, fix broken onboarding card layout, remove command palette focus border, apply soft UI elevation.

**Changes (6 files):**
- `template-gallery.tsx` — raw `<button>` → `<div role="button">` card (avoids Button base style conflict with flex-col layout). Added `shadow-sm`/`hover:shadow-md` + keyboard handler (Enter/Space).
- `build-wizard.tsx` — custom `fixed inset-0 z-[100]` modal → shadcn `Dialog`/`DialogContent` (overlay, centering, escape handled by Dialog). Internal structure unchanged.
- `chat-view.tsx` — 3 onboarding cards (`<Button variant="outline">`) → `<div>` cards. Fixes content overflow caused by Button base `inline-flex items-center justify-center` conflicting with `flex-col`. Grid gap 3→4, added `shadow-sm`/`hover:shadow-md`, `rounded-lg`.
- `command-palette.tsx` — Added `outline-none focus:outline-none focus:ring-0 focus:border-transparent` to `<Command>` + input container div. Removes focus border when typing.
- `globals.css` — Added `[data-cmdk-root]:focus-visible { outline: none !important }` exemption from global `*:focus-visible` rule.
- `resumes/page.tsx` — Resume collection cards: `rounded-sm` → `rounded-lg`, added `shadow-sm`/`hover:shadow-md` (soft elevation on interactive cards only — NOT on PDF previews).

**Intentional non-conversions:** `job-search-panel.tsx` checkbox/radio inputs (lines 980, 993) remain native — shadcn Input is text-only. Hidden `<input type="file">` in chat-view + upload-modal remain raw.

**Verification:**
- `npx tsc --noEmit` — 0 errors
- `pnpm lint` — 0 errors
- `pnpm build` — succeeds

## 2026-07-23 — shadcn/ui Migration (Finish Remaining Raw Elements)

**Improvement:** Completed migration of all remaining raw `<button>`, `<input>`, and `<textarea>` HTML elements to the existing shadcn-style ui components (`~/components/ui/button`, `~/components/ui/input`, `~/components/ui/textarea`). The ui component suite was already built on `@base-ui/react` with correct CVA variants — no shadcn CLI install was needed (would have overwritten working Base UI wrappers with Radix-based ones, breaking 28 importers).

**Files changed (19 files, -777/+666 lines net reduction):**
- `settings/page.tsx` — 10 buttons → Button (variants: default, outline, ghost, link, destructive), 7 inputs → Input. Toggle switches + tab nav left as raw (intentional custom UI).
- `cover-letter/page.tsx` — 6 buttons → Button, 2 inputs → Input, 3 textareas → Textarea. Mode/language selector pills + saved letter list items + native `<select>` + hidden file input left as raw (intentional).
- `resumes/page.tsx` — 7 buttons → Button (default, ghost, outline, icon variants).
- `login/page.tsx` — 2 buttons → Button (default submit + outline Google OAuth), 2 inputs → Input.
- `register/page.tsx` — 2 buttons → Button, 3 inputs → Input.
- `forgot-password/page.tsx` — 1 button → Button, 1 input → Input.
- `reset-password/page.tsx` — 1 button → Button, 2 inputs → Input.
- `pricing/page.tsx` — 3 buttons → Button (outline + default).
- `settings/billing/page.tsx` — 3 buttons → Button (outline + default).
- `search/RoleAutocomplete.tsx` — 1 input → Input, 2 buttons → Button (ghost icon + ghost list items).
- `search/LocationAutocomplete.tsx` — 1 input → Input, 2 buttons → Button (ghost icon + ghost list items).
- `interview-summary.tsx` — 2 buttons → Button (outline + default).
- `interview-view.tsx` — 1 button → Button (outline).
- `pipeline/job-notes.tsx` — 1 textarea → Textarea.
- `admin/source-health.tsx` — 1 button → Button (link).
- `error.tsx`, `(app)/error.tsx`, `(app)/admin/error.tsx` — 1 button each → Button.
- `global-error.tsx` — SKIPPED (uses inline styles, not Tailwind — Button component would break).

**Intentional non-conversions:** CSS toggle switches, segmented control pills, tab underline nav, saved-letter list items, native `<select>`, hidden file inputs, template selection cards, global-error inline-styled button.

**Verification:**
- `npx tsc --noEmit` — 0 errors
- `pnpm lint` — 0 errors (2 pre-existing warnings unchanged)
- `pnpm build` — succeeds

## 2026-07-21 — Global UpgradeModal for all feature-limit 402s (chat + resume)

**Improvement:** The UpgradeModal is now a single global popup triggered via the UI store, covering both the chat limit (15/day) and the resume limit (3 total). Previously the chat had a local modal and resume creation used a toast — now both use the same prominent popup.

**Changes (5 files):**
- `src/app/hooks/use-ui.ts` — Added `upgradeModal: { open, data }` state + `openUpgradeModal(data)` / `closeUpgradeModal()` actions to the global Zustand store. Accessible from anywhere via `useUIStore.getState()`.
- `src/app/lib/resume-limit.ts` — `handleResumeLimitError()` now calls `useUIStore.getState().openUpgradeModal(...)` instead of showing a sonner toast. Works from react-query `onError` (no React context needed).
- `src/app/components/chat/chat-view.tsx` — Replaced local modal state with `useUIStore.openUpgradeModal`. Removed the `<UpgradeModal>` render (now global). Error-clear effect simplified.
- `src/app/[locale]/(app)/layout.tsx` — Renders one `<UpgradeModal>` in `AppShell`, listening to the store. Available on every authenticated page.
- `tests/unit/resume-limit.test.ts` — Updated mocks to assert `openUpgradeModal` is called instead of `toast.error`.

**Verified:** tsc 0 errors, eslint 0 errors/warnings, 119 tests pass.

## 2026-07-21 — Chat limit 402 → UpgradeModal popup

**Bug:** POST `/api/chat` returned 402 (Free plan: 15 messages/day) silently — no UI feedback. User saw nothing actionable in the chat.

**Fix (2 files):**
- `src/app/components/ui/upgrade-modal.tsx` (new) — Reusable `UpgradeModal` popup with: limit text, Pro pitch ($4/mo or $29/yr), three CTAs (View Plans → /pricing, Go to Billing Settings → /settings/billing, Maybe later). Full keyboard/focus-trap support matching `ConfirmDialog` pattern.
- `src/app/components/chat/chat-view.tsx` — Custom `fetch` wrapper on `DefaultChatTransport` intercepts 402 responses before the AI SDK processes them; parses `{ feature, limit }` from body and opens the `UpgradeModal`. Also destructures `error` + `clearError` from `useChat` to clear the stuck error state when the modal opens.

**Verified:** tsc 0 errors, lint 0 errors on changed files, 119 tests pass.

## 2026-07-21 — Fix resume-creation 402 error UX + gate bugs

**Bug:** POST `/api/resumes` returned HTTP 402 (Payment Required) for Free-plan users at the 3-resume limit. The block fired correctly per AGENTS.md rule #12, but the client showed a raw/generic error instead of an actionable upgrade prompt, and two latent gate bugs were present.

**Root cause:**
1. `ApiClient.request` threw a generic `Error`, discarding the 402 status — callers couldn't distinguish limit errors from other failures.
2. The route duplicated the limit logic with a hardcoded `>= 3`, bypassing the shared `gateFeature('resume_create')` helper (would silently drift if the limit changed).
3. `getResumeCount()` in `plan.ts` counted soft-deleted resumes (no `deletedAt IS NULL` filter), inflating the count shown on `/settings/billing`.

**Fix (5 files + 1 test):**
- `src/app/lib/api-client.ts` — added typed `ApiError` (carries `status` + parsed body); `request()` throws it. Backward compatible (extends Error).
- `src/app/lib/plan.ts` — `getResumeCount()` now filters `isNull(resumes.deletedAt)`.
- `src/app/api/resumes/route.ts` — replaced inline hardcoded gate with `gateFeature(user.id, 'resume_create', ...)`; preserved the `isBase ?? true` guard so tailored variants never hit the limit.
- `src/app/lib/resume-limit.ts` (new) — `handleResumeLimitError(err)` detects a 402 and shows a sonner actionable toast (Upgrade action → `/pricing`, with "delete a resume" hint). Framework-agnostic.
- `src/app/hooks/use-resumes.ts` — `useCreateResume` `onError` calls the helper; covers all callers (resume-detail, cover-letter page, upload-modal, chat-view).
- `tests/unit/resume-limit.test.ts` (new) — 10 tests: 402/non-402/generic-error handling, custom limit/upgradeUrl, gate blocks free-at-3, allows free-under-3, pro/admin never blocked, soft-delete exclusion path.

**Verified:** `npx tsc --noEmit` 0 errors; `pnpm lint` 0 errors (2 pre-existing unrelated warnings); `pnpm test` 14 files / 119 tests pass.

**Commit:** see git log.

## 2026-07-21 — Fix /api/preview-pdf 500 (Unknown font format) in production

**Bug:** Resume preview showed blank in prod. `/api/preview-pdf` returned 500 `Error: Unknown font format` from `@react-pdf/renderer`'s fontkit.

**Root cause:** Vercel team SAML SSO enabled on deployment-specific URLs. When Lambda self-fetched fonts via `https://${VERCEL_URL}/fonts/...`, Vercel 302-redirected to SSO interstitial (HTML page). fontkit received HTML bytes instead of TTF, threw.

**Fix (1 file):**
- `src/app/components/resume/templates/shared-pdf.ts` — `fontSrc()` now prefers `VERCEL_PROJECT_PRODUCTION_URL` (prod alias, not SSO-gated) over `VERCEL_URL`. Hardcoded fallback `job-for-sure-ecru.vercel.app` for preview deployments where prod env var isn't set. Removed `VERCEL_URL` branch entirely.

**Verified:** tsc 0 errors, lint 0 warnings.

**Commit:** `9be52d6`

## 2026-07-21 — Quick Mode Fixed ThemeProvider maximum update depth error

- **[Quick Mode]** Fixed ThemeProvider maximum update depth error (theme-provider.tsx useEffect deps [theme]→[])

## 2026-07-22 — [Quick Mode] Remove auto-focus ring from chat input bar

- **Changes:**
  - `src/components/agent-elements/input-bar.tsx` — Removed `peer-focus:opacity-100` from the focus ring div; only `peer-focus-visible:opacity-100` remains, so the ring only appears on keyboard (Tab) navigation.
  - `src/app/components/chat/chat-view.tsx` — Added explicit `autoFocus={false}` to `<InputBar>` to prevent inherited auto-focus behavior.
- **Verification:** `tsc --noEmit` 0 errors, `pnpm lint` 0 errors, commit `3f36da0`.

## 2026-07-18 — Replace ADMIN_EMAIL with Better Auth admin plugin (RBAC)

**Task:** Refactor ad-hoc env-based admin check into production-grade DB-backed RBAC.

**Decision:** Use Better Auth's official `admin()` plugin (not hand-rolled role column). Plugin gives role/banned/banReason/banExpires columns on `user` + `impersonatedBy` on `session`, plus 12 admin API endpoints, ban/impersonate capability, and automatic `role` exposure in session.

**Admin scope:** Monitor-only. Admins are locked out of normal user features (/chat, /resume, /ats, /applications). Admins see only the /admin dashboard. To debug user issues, admins use impersonation.

**Changes:**
- `src/app/lib/auth.ts` — added `admin()` plugin + `customSyntheticUser` (required because `requireEmailVerification` is on)
- `src/app/lib/auth-client.ts` — added `adminClient()` plugin
- `src/app/lib/schema.ts` — added 4 columns to `user`, 1 to `session`
- `src/app/lib/auth-helpers.ts` — added `requireAdmin()`, `requireUser()`, `requireAdminApi()`; expanded `SessionUser` type
- `src/app/[locale]/(app)/admin/page.tsx` — uses `requireAdmin()` (replaced inline email check)
- `src/app/api/jobs/source-health/route.ts` — uses `requireAdminApi()` (replaced inline email check)
- `src/app/api/auth/is-admin/route.ts` — **deleted** (replaced by `session.user.role`)
- `src/proxy.ts` — added `/admin` to `protectedRoutes`
- `src/app/[locale]/(app)/layout.tsx` — `AuthGuard` now redirects admin→/admin and non-admin→/chat based on `session.user.role`
- `src/app/components/layout/sidebar.tsx` — admin sees monitor-only nav (Admin Dashboard link only); users no longer see Admin link. Reads role from session, not from deleted `/api/auth/is-admin` endpoint.
- `drizzle/0004_big_crystal.sql` — schema migration + bootstrap seed for `longpantorn@gmail.com` → `role = 'admin'`
- `.env.example` + `.env.local` — removed `ADMIN_EMAIL`

**Bootstrap:** One-time SQL UPDATE in migration sets `longpantorn@gmail.com` to `admin`. Future admins added via `auth.api.setRole()` or admin UI.

**Verification:**
- Tests: 13 files, 109 passed (added 9 tests for new helpers)
- TypeScript: 0 errors
- Lint: 0 errors, 204 pre-existing warnings (unchanged)
- DB: schema pushed to Neon, founder's role confirmed as `admin`

## 2026-07-18 — Stripe billing integration (Free/Pro)

**Task:** Add Stripe billing with Free/Pro tiers, feature gating on all 5 API routes, usage tracking, and billing UI.

**Decision:** Free ($0) with generous daily limits plus Pro ($4/mo or $29/yr). Feature gates on API routes check daily/weekly/total limits via `gateFeature()` + `recordUsage()` in `plan.ts`. Resume limit uses `resumes` table count (not usage events). Fail-open on Stripe fetch errors.

**Changes:**
- `src/app/lib/schema.ts` — added `plan`, `stripe_customer_id`, `plan_updated_at` to `users`; `subscriptions` table; `usage_events` table
- `src/app/lib/plan.ts` — `getEffectivePlan`, `checkLimit`, `recordUsage`, `getUsageBreakdown`, `getUserPlan`, `gateFeature`
- `src/app/lib/stripe.ts` — Stripe client singleton (server) + `createStripeClient` (browser)
- `src/app/lib/with-auth.ts` — appends `plan` from `getUserPlan()` to user context
- `src/app/api/stripe/webhook/route.ts` — signature-verified webhook handler; UPSERTs subscriptions, syncs `user.plan`
- `src/app/api/billing/checkout/route.ts` — creates Stripe Checkout session
- `src/app/api/billing/portal/route.ts` — redirects to Stripe Customer Portal
- `src/app/api/billing/subscription/route.ts` — returns current subscription status
- `src/app/api/billing/cancel/route.ts` — cancels subscription at period end
- `src/app/[locale]/(marketing)/pricing/page.tsx` — public pricing page with Free/Pro cards
- `src/app/[locale]/(app)/settings/billing/page.tsx` — billing management with usage bars
- `src/components/billing/upgrade-wall.tsx` — upgrade prompt modal
- `src/components/billing/usage-indicator.tsx` — usage badge component
- `src/app/api/chat/route.ts` — gate feature: chat (15/day free)
- `src/app/api/ai/cover-letter/route.ts` — gate feature: cover_letter (3/week free)
- `src/app/api/ai/ats-match/route.ts` — gate feature: ats_match (5/day free)
- `src/app/api/ai/interview/route.ts` — gate feature: interview (3/week free)
- `src/app/api/resumes/route.ts` — gate feature: resume_create (3 total free, checks resumes table)
- `drizzle/0005_grey_kitty_pryde.sql` — schema migration
- `scripts/setup-stripe.mjs` — creates Pro Monthly ($4) and Pro Yearly ($29) price IDs
- `tests/unit/with-auth.test.ts` — added `getUserPlan` mock
- `tests/unit/ats-match.test.ts` — added `getUserPlan`, `gateFeature`, `recordUsage` mocks
- `tests/unit/interview.test.ts` — added `getUserPlan`, `gateFeature`, `recordUsage` mocks
- `.env.example` — added Stripe variables
- `AGENTS.md` — added billing architecture rules and plan tiers

**Skipped:** `pnpm db:setup-stripe` already run manually. `stripe listen` for webhook testing deferred.

**Verification:**
- Tests: 13 files, 109 passed (unchanged count)
- TypeScript: 0 errors
- Lint: 0 new errors (all pre-existing)
- DB: migration 0005 pushed to Neon, founder seeded as `plan='pro'`
- Stripe: products + prices created via setup script
- Commit: `b1e2589` pushed to `main`

## 2026-07-18 — Bugfix execution plan (33 bugs)

**Task:** Implement all 33 fixes from `docs/BUGFIX_EXECUTION_PLAN.md` across 9 phases.

**Verification:**
- `tsc --noEmit`: 0 errors
- `pnpm lint`: 0 errors (200 pre-existing warnings)
- `pnpm test:unit`: 109/109 passed

**Phases completed:**
1. **Schema** (6 fixes) — unique constraints, score precision, migration generated
2. **Plan & Limits** (3 fixes) — UTC dates, resume count, fail-open recordUsage
3. **Auth** (2 fixes) — safe env access, trustedOrigins warning
4. **AI Providers** (3 fixes) — dead check removed, cancel() return, sanitized logs
5. **API Routes** (10 fixes) — withAuth wrapping, ownership checks, transactions, try/catch
6. **Frontend** (8 fixes) — stable keys, functional updaters, timer cleanup, speech lifecycle
7. **Store** (1 fix) — stale comment
8. **ESLint** (1 fix) — Node globals for scripts
9. **Verification** — all 3 checks pass

## 2026-07-18 — Pre-deploy audit + showstopper fixes (Wave 1)

**Task:** Brutal pre-production audit + fix all blockers before real users.

**Audit scope:** 3 parallel subagent audits covered all 31 API routes, all frontend components, security/config/deploy. Found 50+ issues beyond the existing BUGFIX_EXECUTION_PLAN.md (which had 33 verified bugs, all applied in working tree).

**Wave 1 showstoppers fixed (7):**
- **S1** — `.env.example` had wrong Stripe var names (`NEXT_PUBLIC_STRIPE_PRO_*` vs code's `STRIPE_PRICE_*`) → Pro checkout would 500 on fresh deploy. Fixed.
- **S2** — `/api/auth/*` had ZERO rate limiting (brute-force, signup email-bombing). Added IP-based 10/min limiter + `getClientIp()` helper.
- **S3** — `/api/billing/cancel` was not wrapped with `withAuth` (CSRF hole — any site could cancel a user's Pro plan). Wrapped.
- **S4** — `/api/user/account` DELETE hard-deleted user without canceling Stripe subscription (customer charged forever, no self-serve). Now cancels active subs first.
- **S5** — `BETTER_AUTH_SECRET` was never validated at startup → silent session invalidation on serverless cold starts. Added fail-fast check.
- **S6** — Cover-letter page fabricated `role: 'Software Engineer'` + `skills: ['JavaScript','Git']` on parse failure (violates project rule, misleads non-tech users). Dropped.
- **S7** — `InlineAddForm` was defined INSIDE `ApplicationsView` (textbook React anti-pattern, input lost focus on every keystroke). Hoisted to module scope with explicit props.

**Verification:** tsc 0 errors, 109/109 tests pass, lint 0 errors.

**Deferred to next sprint (Wave 2):**
- H1/H2: recordUsage runs before AI call (users billed for failed chats/interviews)
- H3: 4 routes missing resumeId ownership check
- H4: PDF Content-Disposition filename injection
- H6: 14+ files use raw next/navigation router (bypass i18n)
- H7-H15: various high-priority frontend bugs (see audit report)
- Medium/Low: a11y gaps, memory leaks, `any` typing, XSS surface in translations

**Outcome:** Production blockers cleared. Codebase is now safe to deploy. Wave 2 bugs are real but not deploy-blocking — they're functional/UX issues for the first sprint.

## 2026-07-19 — Wave 2 bugfixes (commit efb1432)

- **recordUsage ordering**: chat + interview routes moved billing AFTER AI success (users no longer billed for failed AI calls)
- **Ownership checks**: `userOwnsResume()` helper added to plan.ts; cover-letters POST, applications POST, ai/cover-letter POST, ai/interview save now verify the resumeId belongs to the authenticated user (defensive — blocks future JOIN-based leaks)
- **ATS index-merge fix**: tailor merge now preserves original experiences AI dropped, always trusts original dates (factual), handles length mismatch safely
- **i18n router**: 22 files converted from raw `next/link` + `next/navigation` to `~/i18n/routing` (auth, marketing, app pages, components)
- **Silent-success fetch**: 6 locations now check `res.ok` and surface real error messages (cover-letter-editor, interview-setup, cover-letter page)
- **Theme white-flash**: inline pre-hydration script in root layout applies `dark` class before paint; ThemeProvider syncs with pre-applied class
- **Mobile sidebar**: overlay strengthened (bg-black/40), body scroll lock added, aria-modal/role attributes added
- **ConfirmDialog a11y**: focus trap, return focus to trigger, role/aria labels, body scroll lock
- **Verified**: tsc 0 errors, lint 0 errors, tests 109/109 pass
- **Commit**: efb1432

## 2026-07-19 — Fix /api/preview-pdf 500 in production (Vercel)

**Bug:** Resume viewer showed blank in prod. `/api/preview-pdf` returned 500 with
`ENOENT: no such file or directory, open '/var/task/public/fonts/inter-regular.ttf'`.

**Root cause:** Vercel's serverless Lambda does NOT mount `/public/` on its
filesystem. `shared-pdf.ts` was returning `process.cwd()/public/fonts/...` on the
server, which doesn't exist at `/var/task/public/` inside the function sandbox.

**Fix (2 files):**
- `src/app/components/resume/templates/shared-pdf.ts` — `fontSrc()` now returns
  `https://{VERCEL_URL}/fonts/{file}` on the server (falls back to
  `VERCEL_PROJECT_PRODUCTION_URL`, then to filesystem path for local dev).
- `src/app/api/preview-pdf/route.tsx` — `maxDuration` bumped 10 → 30 to cover
  cold-start + font fetch headroom on Pro.

**Verified:** `tsc --noEmit` clean on both files.

## 2026-07-22 — Job Card — Salary Range + Experience Needed

**Task:** Add salaryMin/salaryMax/salaryCurrency to JobResult; add experienceYears to JobResult; populate from all source adapters; display as visually distinct pills in UI.

**Changes (31 files):**
- `src/app/lib/job-sources/types.ts` — added 4 new optional fields + `extractExperienceYears()` regex utility
- Ashby (fix): added `AshbyCompensation` interface; compensation now actually read from API
- Greenhouse (expand): added experience/seniority metadata search + employment type from metadata
- JSearch, Himalayas, Adzuna, RemoteOK: structured salary fields + experienceYears
- Remotive, Arbeitnow, Themuse, JobbKK, JobsDB-REST, Apify-Indeed, Apify-JobsDB, Apify-LinkedIn: regex-based experienceYears
- LinkedIn-Guest: criteria HTML parsing for experience years (detail) + title scan (list)
- `index.ts` (lean cache): 4 new fields in cache object
- `job-utils.ts` (`scoredJobToPipelineJob`): pass through 4 new fields in jobData
- 3 UI components: `job-search-panel.tsx`, `job-preview.tsx`, `job-detail-panel.tsx` — green salary pill with `$` icon + `Briefcase` experience years pill
- `tests/unit/extractExperienceYears.test.ts` — 11 test cases

**Verified:** `tsc --noEmit` 0 errors, `pnpm lint` 0 errors (2 pre-existing warnings), `pnpm vitest run` 15 files / 130 tests pass, `npx next build` succeeds.

- **133 `no-explicit-any` fixed**: introduced PdfStyle (react-pdf), ParsedResumeFields normalizer (shared in resume-normalize.ts), InterviewSessionRow, Stripe SDK types, proper useApp typing, etc.
- **49 `no-unused-vars` cleaned**: removed dead code (avgScore, injectKeywords, saveChanges, toggleSourceSet, handleDragOver) and unused imports across 20+ files
- **17 `react-hooks/exhaustive-deps` resolved**: documented intentional exclusions with inline comments + eslint-disable; wrapped changes/accepted in useMemo in tailor-review-panel
- **agent-elements library**: file-level eslint-disable (vendored; consumes arbitrary tool schemas by design)
- **Shared helper**: `src/app/lib/resume-normalize.ts` now centralizes AI-parsed resume normalization (previously duplicated in 3 components)
- Verified: tsc 0 errors, lint 0 warnings (was 199), tests 109/109 pass
- Commit: 65c0aac

### 2026-07-22 [Quick Mode] fix: support Thai language in experience years extraction

- **Changes:**
  - `src/app/lib/job-sources/types.ts` — Rewrote `extractExperienceYears()` with sequential English + Thai pattern matching. Thai patterns use direct keyword matching (avoiding `\b` which doesn't work with Thai script). Supports: range (`ประสบการณ์ 3-5 ปี`), years-up (`X ปีขึ้นไป`), at-least (`อย่างน้อย X ปี`), not-less-than (`ไม่น้อยกว่า X ปี`), and context keywords (`ประสบการณ์`, `อายุงาน`, `อายุการทำงาน`). Bare `อายุ X ปี` (age) correctly returns undefined.
  - `src/app/lib/job-sources/linkedin-guest.ts` — Removed useless `extractExperienceYears(title)` call from list page mapping (job titles never contain experience years). Import kept for detail page usage.
  - `tests/unit/extractExperienceYears.test.ts` — Added 11 Thai test cases across 3 describe blocks (English/Thai/mixed). Covers all Thai patterns plus age false-positive guard.
- **Verification:** `pnpm vitest run` — 22 tests pass (was 11). `npx tsc --noEmit` — 0 errors.

### 2026-07-23 Blueprint UX — 3 remaining fixes
- sidebar.tsx: removed unused `router`/`useRouter` (lint fix), added `bg-grid-blueprint` to sidebar aside
- job-detail-panel.tsx: footer bg-card/90 → bg-card/95
- Verified: tsc 0 errors, lint 0 errors (2 pre-existing warnings only)

## 2026-07-23 — Blueprint UX Visual Overhaul (specs/blueprint-ux-visual-overhaul.md)

**Task:** Full CSS token refresh from warm paper to cool canvas palette + command palette feature.

**Changes (8 files):**
- `src/app/globals.css` — Light/dark mode color tokens updated to cool canvas palette (F8F9FA background, 0F1115 foreground, E0E3E8 borders, 8B98E0 primary dark). New `bg-grid-card` class for card interiors. New `.label-bracket` utility class with `[ ... ]` pseudo-element wrappers. Refined shadow tokens (softer/lighter).
- `src/app/[locale]/(app)/resumes/page.tsx` — Removed `hover:shadow-md` from resume cards. Added `bg-grid-card` + dashed border to empty state container.
- `src/app/components/pipeline/job-detail-panel.tsx` — Changed 4 AI tool buttons (Tailor Resume, Cover Letter, ATS Match, Interview) from solid primary to muted ghost style (`bg-muted border`).
- `src/app/components/layout/sidebar.tsx` — Swapped `label-mono` → `label-bracket` on NavSection, MY RESUMES, ADMIN, and ACCOUNT section headers. Removed inline 11px font-size style (now via `.label-bracket`).
- `src/app/components/layout/navbar.tsx` — Added `Search` import + ⌘K trigger button with search icon and keyboard shortcut badge.
- `src/app/components/layout/command-palette.tsx` — NEW: global ⌘K command palette with Navigation (7 items) and Actions (2 items) groups, cmdk-backed search, keyboard nav, footer hints.
- `src/app/[locale]/(app)/layout.tsx` — Mounted `<CommandPalette />` in AppShell root.
- Added `cmdk` dependency (v1.1.1).

**Verification:** tsc 0 errors, lint 0 errors (2 pre-existing warnings), build succeeds, 141 tests pass.

## 2026-07-23 — [Quick Mode] Fix AGENTS.md paths and stale state management references

**Task:** Fix AGENTS.md to match actual codebase structure — all `app/` → `src/app/` paths, remove dead `store.tsx` references, update state management to Zustand/TanStack Query.

**Changes:**
- `AGENTS.md` — Fixed Tech Stack table (State: Zustand v5 + Immer + TanStack Query v5), rebuilt Directory Structure tree with `src/` root, fixed all 7 path references in Coding Rules (`app/` → `src/app/`), fixed `proxy.ts` location, fixed path aliases (`~/` → `./src/app/`, `@/` → `./src/`), removed `store.tsx` from tree.
- `docs/architecture.md` — Updated State Management section from "Custom React context store" to Zustand + TanStack Query with actual file paths, fixed Auth Flow and Guardrail path references.
- `docs/adr/003-ai-failover.md` — Fixed `app/lib/ai-providers.ts` → `src/app/lib/ai-providers.ts`.
- `docs/adr/004-tech-job-search.md` — Fixed `app/lib/job-sources/` → `src/app/lib/job-sources/`.
- `docs/glossary.md` — Fixed ~25 stale `app/` → `src/app/` paths across all sections, replaced `useAppStore()`/`store.tsx` references with Zustand/useUIStore equivalents.

**Outcome:** Success. All path references verified against actual filesystem. Committed.

---

## 2026-07-23 — [Quick Mode] Document AI content XSS sanitization

- **Task:** Audit finding flagged AI content rendered via `streamdown` without sanitization.
- **Finding:** `streamdown` already sanitizes HTML by default via `rehype-sanitize` (part of its rehype plugin pipeline). The schema uses `hast-util-sanitize`'s strict GitHub-style allowlist: ~40 safe tags, no event handlers, no dangerous protocols, clobber prefix applied.
- **Changes:**
  - `src/components/agent-elements/markdown.tsx` — Added inline documentation of the built-in sanitization. No new dependency or wrapper needed.
- **Verification:** `npx tsc --noEmit` 0 errors, `pnpm build` succeeds, `pnpm lint` 0 new warnings.

---

### 2026-07-23 [Quick Mode] style(applications): complete neumorphism on kanban board, job detail panel, timeline, notes

- **Changes:**
  - `applications-view.tsx` — DroppableColumn → `neuro-inset` + `ring-1 ring-primary/40` drag-over; Header bar/Column header → `neuro-divider`; Paste URL + InlineAddForm inputs → `neumorphic` prop; DraggableJobCard → `hover:-translate-y-0.5` lift
  - `job-detail-panel.tsx` — SheetHeader/SheetContent footer → `neuro-divider`; Salary tag → `bg-success-soft` (no border); Location/experience/posted tags → `neuro-surface` (no `border-border`)
  - `timeline.tsx` — Removed unused `notify` import; Dots → `neuro-surface`; Vertical line → `bg-muted-foreground/20`
  - `job-notes.tsx` — Textarea → `neumorphic` prop; Removed `border-border bg-background`
- **Verification:** `npx tsc --noEmit` ✓, `pnpm lint` ✓ (0 errors, 1 pre-existing warning), `pnpm build` ✓
- **Outcome:** Success

## 2026-07-23 — Neumorphism UX Polish (All 3 Phases)

**Task:** Implement neumorphism UX polish spec — fix focus states, remove conflicting rings, remove ⌘K palette, remove hard borders, convert form elements to neumorphic, add neumorphic prop to all inputs across the app.

**Phase 1 — Critical UX Fixes:**
- `globals.css` — Scoped `textarea:focus-visible:not(.neuro-inset)`, added `.neuro-inset:focus-visible`/`:focus-within` stacked shadow+ring focus state, added `.neuro-divider` utility, added dark-mode focus state.
- `input.tsx` — Removed `focus-visible:ring-3 focus-visible:ring-ring/50` from neumorphic variant (CSS handles it now).
- `textarea.tsx` — Same ring removal.
- `layout.tsx` — Removed `CommandPalette` import and usage.
- `navbar.tsx` — Removed `Search` import, removed ⌘K button block, removed `border-b border-border` from header, removed `border-r border-border` from brand area.
- `sidebar.tsx` — Removed `border-r border-border` from aside.
- `resume-detail.tsx` — Removed `border-b border-border` from header, template gallery, mobile toggle; replaced `border-r border-border` with subtle shadow on review panel; removed `border-t border-border` from mobile fallback/inner borders and Co-Pilot drawer header.

**Phase 2 — Resume Detail Editor Polish:**
- Converted SortableItem, TagInput, DragOverlay, custom section input container to `neuro-inset`/`neuro-card`.
- Added `neumorphic` prop to all ~26 `<Input>` and `<Textarea>` in renderEditorSection + custom sections.
- Removed `border-t border-border/50` from EditableList divider, Add Section divider, custom section divider.

**Phase 3 — Rest of App Neumorphic Consistency:**
- `cover-letter-editor.tsx` — Added `neumorphic` to 4 inputs/textareas, removed `border-border` from panel, removed `border-b` from action bar, removed `border-b/50` from paper divider.
- `ats-view.tsx` — Removed `border-border` from left panel, replaced `border border-border/50` with shadow on suggestion cards.
- `settings/page.tsx` — Added `neumorphic` to all 7 inputs.
- `interview-setup.tsx` — Added `neumorphic` to both Input components.
- Auth pages (login, register, forgot-password, reset-password) — Added `neumorphic` to all 8 Input components.
- `navbar.tsx` — Wrapped `<Image>` in `neuro-icon-well` container for logo.

**Verification:**
- `npx tsc --noEmit` — 0 errors
- `pnpm lint` — 0 errors (2 pre-existing warnings)
- `pnpm build` — Success

**Outcome:** Success. All 3 phases implemented and verified.

- **Task:** Upgrade next, drizzle-orm, and sharp to patch all 6 HIGH CVEs from `pnpm audit`.
- **Changes:**
  - `next` 16.2.10 → 16.2.11 — patches 4 HIGH CVEs (middleware bypass GHSA-6gpp-xcg3-4w24, DoS GHSA-m99w-x7hq-7vfj, SSRF in Server Actions GHSA-89xv-2m56-2m9x, SSRF in rewrites GHSA-p9j2-gv94-2wf4)
  - `drizzle-orm` 0.42.0 → 0.45.2 — patches HIGH SQL injection CVE (GHSA-gpj5-g38j-94v9)
  - `sharp` 0.34.5 → 0.35.3 via `pnpm-workspace.yaml` overrides — patches 4 HIGH libvips CVEs (CVE-2026-33327/33328/35590/35591)
- **Audit:** 14 vulns (6 high) → 3 vulns (0 high, 2 moderate, 1 low)
- **Remaining (no available fix):** esbuild (moderate, dev tool), postcss (moderate, bundled in next), dompurify (low, transitive from posthog-js/mermaid)
- **Verification:** `npx tsc --noEmit` ✓, `pnpm build` ✓, `pnpm test` 141/141 ✓, `pnpm lint` 0 errors ✓
- **Commit:** `5fcd065`

---

## 2026-07-24 [Quick Mode] fix(cover-letter): make Quick mode input fields larger and more prominent

**Task:** Increase visibility of Quick mode inputs (Company Name, Job Title, Focus/Highlights) on the cover letter page.

**Changes (1 file):**
- `src/app/[locale]/(app)/cover-letter/page.tsx`:
  - Sidebar width: `lg:w-[350px]` → `lg:w-[400px]` for more room
  - Company Name input: `text-[11px] px-2.5 py-1.5` → `text-sm px-3 py-2.5`
  - Job Title input: same sizing bump
  - Focus/Highlights textarea: same sizing bump
  - Labels: `mb-1` → `mb-1.5` on all three Quick mode field labels

**Verification:** `npx tsc --noEmit` ✓ (0 errors). Commit: `9b502d9`.

## 2026-07-24 — fix: decouple logo from animating brand area to prevent position shift on sidebar toggle

**Task:** Fix logo shifting when toggling sidebar (logo was inside an animating `transition-all` brand area div). Decoupled logo by making `<header>` the positioned ancestor instead.

**Changes (1 file):**
- `src/app/components/layout/navbar.tsx` — Topbar header now `relative`; logo `<Link>` and brand text `<Link>` moved from inside brand area div to direct children of `<header>` (absolutely positioned, `z-10`); brand area replaced with empty spacer div using `transition-[width]` only; removed `gap-2` from logo Link.

**Verification:** `npx tsc --noEmit` ✓ (0 errors), `pnpm lint` ✓ (0 errors, 1 pre-existing warning in unrelated file)

## 2026-07-24 [Quick Mode] style: warm-tint neumorphic light shadows to match ivory palette

**Task:** Replace pure white `rgba(255,255,255,0.9/0.95)` in neumorphic `.neuro-*` shadows with warm `rgba(253,251,247,0.9/0.95)` to match `--card: #FDFBF7` and the warm ivory palette. Also update stale comment referencing `#F8F9FA` → `#F7F4EF`.

**Changes (1 file):**
- `src/app/globals.css` — 10× `rgba(255,255,255,0.9)` → `rgba(253,251,247,0.9)`, 2× `rgba(255,255,255,0.95)` → `rgba(253,251,247,0.95)` in `.neuro-card`, `.neuro-inset`, `.neuro-icon-well`, `.neuro-pill`, `.neuro-modal`, `.neuro-chat` selectors. Comment line 412 updated. Dark mode `.dark .neuro-*` untouched.

**Verification:** `npx tsc --noEmit` ✓, `pnpm build` ✓.
**Commit:** `88a0c62`

## 2026-07-24 — Kanban Board Spacing & Typography Calibration

**Task:** Increase padding, gap, and font sizes across the Kanban board for readability. All changes in `applications-view.tsx`.

**Changes (1 file):**
- `src/app/components/pipeline/applications-view.tsx`:
  - **STEP 1**: Job card padding `p-2.5` → `p-4` (3 occurrences: DraggableJobCard, InlineAddForm, DragOverlay)
  - **STEP 2**: Gap between columns `gap-3` → `gap-5`
  - **STEP 3**: Gap between cards `gap-1.5` → `gap-3`, column padding `px-3 pb-2 pt-3` → `px-4 pb-3 pt-4`
  - **STEP 4**: Card title text `text-[11px]` → `text-xs`
  - **STEP 5**: Meta text `text-[9px]` → `text-[11px]` (5 occurrences: score badge, location/salary, date, Open action, Remove action)

**Verification:** `npx tsc --noEmit` ✓ (0 errors), `pnpm lint` ✓ (0 errors, 1 pre-existing warning in unrelated file)

## 2026-07-24 — Warm Tactile Neumorphism Shadows + Kanban Spacing

**Changes:**
- `src/app/globals.css`: Reduced all neuro shadow light opacity (white 0.9→0.35, 0.95→0.4, 0.5→0.35, 0.6→0.35). Warm-tinted all dark shadows from `rgba(0,0,0,0.xx)` to clay-tinted `rgba(74,68,56,0.xx)`. Dark mode untouched.
- `src/app/components/pipeline/applications-view.tsx`: All kanban spacing changes (p-2.5→p-4, gap-3→gap-5, gap-1.5→gap-3, text-[9px]→text-[11px], text-[11px]→text-xs) were already applied from a prior session — verified no additional changes needed.

**Verification:** `npx tsc --noEmit` ✓ (0 errors), `pnpm build` ✓ (success)

## 2026-07-24 — Neumorphic Quick-Action Buttons [Quick Mode]
**Task:** Style chat suggestion chips with neumorphic raised look.

**Changes (1 file):**
- `src/components/agent-elements/input/suggestions.tsx` (line 42): Replaced flat `border border-border bg-transparent` with `neuro-pill` class; bumped `h-7→h-8`, `px-2→px-3`, `gap-1→gap-1.5`, `rounded-[6px]→rounded-lg`, `text-sm→text-xs font-medium`, `transition-colors hover:bg-...→transition-shadow`.

**Verification:** `npx tsc --noEmit` ✓ (0 errors), `pnpm build` ✓ (success)

### 2026-07-24 04:28 [Quick Mode] style: lower white highlight to 0.22 for premium neumorphic depth
- **Changes**: `src/app/globals.css` — 14 replacements: `rgba(253,251,247,0.35)→0.22` (12x), `rgba(253,251,247,0.4)→0.28` (2x) across all `.neuro-*` and `.neuro-chat` classes
- **Verification**: `npx tsc --noEmit` clean, `pnpm build` success
- **Outcome**: Committed 3a9fc0c
